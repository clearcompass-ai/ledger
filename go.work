// Go workspace pinning the modules in this repo.
//
// Only the root module (github.com/clearcompass-ai/ledger) lives
// here. The standalone-witness daemon is hosted in its own
// repository (github.com/clearcompass-ai/standalone-witness); it
// imports the same Attesta SDK (github.com/clearcompass-ai/attesta)
// but its module graph is resolved independently — it never imports
// github.com/clearcompass-ai/ledger.

go 1.25.7

use .
